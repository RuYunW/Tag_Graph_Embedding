'''
Tag-graph embedding generation algorithm
'''

from utils import load_data,LSTM

x = load_data()
order = set()


for k in range(5):  # hop数
    for m in range(3):  # 节点个数
        h[][] =
        h[][] =
        order = h[][] + h[][]
        for u in order:
             h[][] = LSTM(h[][])
        h[][] = Sigma()
z[] = h[][]